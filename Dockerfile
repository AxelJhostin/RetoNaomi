# Dockerfile
FROM node:20-bookworm-slim

WORKDIR /workspaces/retonaomi

# Este Dockerfile es simple porque devcontainers se encargará de la mayoría de las cosas.
# Se usa principalmente para definir la imagen base.